/*
 * Vite + WordPress Theme
 *
 * - Run `npm run dev` for development with HMR (PHP, JS, CSS)
 * - Run `npm run build` for production build
 * - No SVG sprite generation (removed)
 *
 * Update your WordPress theme to enqueue built assets from /dist in production,
 * or use the Vite dev server in development.
 */
